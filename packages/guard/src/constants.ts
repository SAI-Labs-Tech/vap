/** Default SAI Guard HTTP API. Verified live; POST /v1/protect is not deployed yet. */
export const DEFAULT_BASE_URL = "https://vap-api.saiwallet.ai";

export const PROTECT_PATH = "/v1/protect";
export const CAPABILITIES_PATH = "/v1/capabilities";
export const HEALTH_PATH = "/health";

export const DEFAULT_TIMEOUT_MS = 30_000;

export const DOCS_URL = "https://guard.sai-labs.pro/";
export const SDK_VERSION = "0.1.0";
