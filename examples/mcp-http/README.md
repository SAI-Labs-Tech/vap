# SAI Guard MCP — Streamable HTTP

```bash
npx tsx examples/mcp-http/index.ts
```

Optional:

```text
MCP_AUTH_TOKEN
SAI_GUARD_API_KEY
SAI_GUARD_BASE_URL
PORT
```

Do not hardcode `mcp.guard.sai-labs.pro`. Supply host authentication in the process that fronts this server.
